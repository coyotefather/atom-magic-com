/**
 * page.tsx — Cookies Policy (/cookies)
 *
 * Static legal/policy page. Content is a placeholder ("...") — fill in with
 * actual cookie policy text before launch.
 */
import Header from '@/app/components/common/Header';

const Page = () => {
	return (
		<main className="notoserif">
			<Header name="Cookies">
				  ...
			</Header>

		</main>
	);
};

export default Page;
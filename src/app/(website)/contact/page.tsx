import Header from '@/app/components/common/Header';

const Page = () => {
	return (
		<main className="notoserif">
			<Header name="Contact">
  				Get in touch.
			</Header>
			...
		</main>
	);
};

export default Page;